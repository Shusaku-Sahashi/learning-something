# learning-something

このリポジトリの用途:

1. Claudeなどのllmに特定の分野を学習するための資料を作成してもらい、依頼者がその資料を通して知識を深めていくために使用する。
2. 1つのTopicにつき1つのディレクトリを使用する。例えば「SQLiteの内部構造を知りたい」「jqの基礎を身につけるためのBootキャンプを作成して、jqを日常生活で使いこなせるようにする」といった目的ごとにディレクトリを作成する。

## 学習資料作成時のルール

学習資料を作成してほしいという依頼があった場合は、`/grill-with-docs` を使って依頼内容を具体化してから資料作成に着手する。

## 学習資料作成の流れ
1. `/grill-with-docs` を使って依頼内容を具体化する。
2. 依頼内容をもとに適切なディレクトリを作成し、学習資料を作成する。
3. 作成した学習資料の内容で、リポジトリのREADME.mdを更新する。
4. README.mdの一覧には各コースのステータス（`実施中` または `完了 (完了日)`）を記載する。

## Markdown の callout・折りたたみのルール

1. 折りたたみ（`<details>`）を追加する際は、範囲がわかるように `> [!NOTE]` などの
   callout で囲みつつ、その内側に `<details><summary>...</summary>...</details>`
   を配置する（`>` を各行の先頭に付けて callout のブロッククォート内に収める）。
2. Callout の作成を依頼された場合は、内容に応じて適切な種類（`[!NOTE]`,
   `[!TIP]`, `[!IMPORTANT]`, `[!WARNING]`, `[!CAUTION]`）を付けて作成する。
